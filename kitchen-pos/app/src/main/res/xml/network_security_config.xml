<?xml version="1.0" encoding="utf-8"?>
<!-- The kitchen POS connects to a hotel's on-premises backend, which is
     typically served over plain HTTP on the local network. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
