<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; the real styling lives in Compose. A dark splash matches
         the slate-900 POS board so there is no white flash on launch. -->
    <style name="Theme.KitchenPos" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#0F172A</item>
        <item name="android:navigationBarColor">#0F172A</item>
        <item name="android:windowBackground">#0F172A</item>
    </style>
</resources>
